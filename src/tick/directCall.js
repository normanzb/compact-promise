//>>excludeStart("release");
if (typeof define !== 'function') {
    var define = require('amdefine')(module);
}
//>>excludeEnd("release");

define(function() {
    'use strict';

    return function(func){
        func();
    };
});